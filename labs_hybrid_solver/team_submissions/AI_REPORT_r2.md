# AI_REPORT.md — NVIDIA LABS Hybrid Solver (iQuHACK 2026)

## 0) Executive summary
- Goal: Role-aware use of AI agents to create compounding improvements, and diverse evaluation, avoiding the inefficiencies of unstructured AI.
- AI agents: Claude Code (Opus 4.5) via CLI and ChatGPT Pro  were employed, with Claude focused on Coding and Testing and ChatGPT Pro focusing on Research and problem-solving.

## 1) Agent workflow

**Tool:** ChatGPT Pro

**Development cycle:**
1. Literature Review (ChatGPT was chosen for research because of its superior web search abilities)
2. Apply literature to problem statement
3. Generate recommended repo structure, strategies, and tests
4. Send to Claude for coding
5. Audit the codebase - return to Claude to run experiment
6. Audit the documented experiment records and results (includes ALL details of experiment, not just final results)
7. Send recommendations to Claude


**Tool:** Claude Code (Anthropic CLI) running Claude Opus 4.5.

**Development cycle:**
1. Receive recommended strategies, supporting papers, etc. from ChatGPT
2. Develop Locally
3. Send to ChatGPT for audit
4. Manage Audit from ChatGPT (Claude was specifically chosen for codebase change management due to its superior coding and context retention)
5. Re-deploy
6. Iterate

**Auditing Key Patterns:**

1. Separation of generation and validation is critical. The AI responsible for producing a given artifact should not be the same AI that validates it. In our workflow, major outputs are generated by one AI and then passed to the other for verification. For example, ChatGPT produced the initial pseudocode and strategy, Claude implemented the code and test suite, and both ChatGPT and Claude independently interpreted and reviewed the resulting test outputs.

2. Extensive, machine-readable documentation improves AI effectiveness. Providing a comprehensive, AI-ingestible record of all relevant information produced during code execution enables downstream AIs to more easily reconstruct context, diagnose issues, and reason about experimental outcomes.

3. A shared AI Project Memory enables compounding progress. Throughout development, both AI agents contributed to a shared document used as an AI Project Memory, continuously recording design decisions, modifications, and observations. This created a traceable timeline and a persistent, referenceable handshake between the two AI agents.

## 2) Verification strategy

**Test suite:** 8 test files, 30 test functions (702 lines).

| Layer | What it proves | Tests |
|-------|---------------|-------|
| **Brute-force oracle** | Energy function is correct for N ≤ 8 | `test_energy_smallN_bruteforce.py` (3 tests) |
| **CPU/GPU energy parity** | CuPy RawKernel matches NumPy exactly (integer energy) | `test_energy_cpu_gpu_parity.py` (3 tests) |
| **Delta-energy kernel parity** | All 3 CUDA kernels match naive full-eval | `test_delta_energy_parity.py` (3 tests) |
| **Seeder validity** | Shape (K_out, N), dtype int8, values ±1, determinism | `test_seeders_validity.py` (3 tests) |
| **MTS sanity** | CPU improves seeds, GPU delta matches naive, CPU/GPU convergence parity, stagnation early-exit | `test_mts_sanity.py` (5 tests) |
| **DCQO+ trotter knobs** | Zero-depth invariants hold across all knob combinations | `test_dcqo_plus_trotter_knobs.py` (4 tests) |
| **Post-selection** | Correct tail extraction, quality improvement, sequence preservation, edge cases, diversity metrics | `test_selectors.py` (9 tests) 

**Philosophy:** CPU is the correctness oracle. GPU must match exactly


**Metrics + logging format:**
Each run appends one row to `metrics.csv` and `metrics.jsonl`:
- problem: N
- seeder: name, params, shots, K_out, K_select
- runtime breakdown: compile, sampling, post-select, MTS
- quality: best seed energy, best final energy
- trace summary: length of best-so-far curve, MTS iterations
- diversity (recommended): mean pairwise Hamming distance among MTS starts


## 3) Vibe log

### Wins
- **GPU energy kernel**: CuPy RawKernel with shared-memory tree reduction — 150–250× speedup implemented in a single session. Kernel runs in 31–40 µs constant time regardless of batch size K or sequence length N.
- **Coding**: Claude saves enourmous amounts of time coding
- **PCE paper implementation**: ChatGPT Pro saves enormous amount of time scraping the web for scientifically relevant info
- **Results Interpretation**: AI Results Interpretation workflows save only modest amounts of time, but is great for keeping the humans focused on the big picture, and not being bogged down by details.

### Learnings
- **Detailed plans yield better results**: providing algorithm equations, function signatures, and paper references (rather than vague "implement PCE") produced correct code on first pass.
- **Gate pipeline structures development**: the Gate 0→3 progression naturally catches issues early (CPU correctness before GPU bring-up before full benchmarks).
- **Living revision doc works**: Claude-revisions.md as a shared checklist between human and AI kept work focused and trackable.

### Failures / bugs

- **scipy.optimize.minimize `kwargs` bug**: passed unsupported keyword arguments to L-BFGS-B, caused silent failures. Fixed by using only supported parameters.
-**Early in the project, ChatGPT randomly became convinced that we were using a VQE algorithm to generate the seeds. It had VQE on its mind since I had interacted with it about VQE earlier in the week. Right when that happens, I've learned its important to abandon that chat completely and have the AI generate an AI Handoff file that helps retain wanted context. 
- **PCE max_restarts = K_out (256)**: caused 454s seeding time for N=16. Fixed by capping max_restarts to 30 — sufficient for convergence, reduces runtime significantly.
- **OOM on large-N CUDA-Q seeders**: GPU memory exhaustion with no graceful recovery. Fixed by adding pre-check and exception handler with fallback.

### Context dump
- **EXAMPLE AI HANDOFF (CHATGPT PRO --> Claude) 

HANDOFF SPEC: Generate Full Codebase + Deliverables for NVIDIA LABS iQuHACK 2026 (Phase 2)
1) Project identity and non-negotiables
Context

Challenge: NVIDIA LABS / Low Autocorrelation Binary Sequences (LABS) hybrid solver.

Hackathon: MIT iQuHACK 2026.

Critical design decision (do not violate)

Do NOT embed new ideas inside the Gómez‑Cadavid QE-MTS loop. Treat QE-MTS/DCQO as one seeding baseline and implement all “twist” ideas (PCE, DCQO+) as parallel seeders feeding the same fixed MTS + GPU acceleration. 

Ai Handoff – Paper Research

 

AI_HANDOFF_paper_implementation

Fixed vs variable

Fixed across all experiments

LABS objective (energy / merit factor)

Classical solver: Memetic Tabu Search (MTS)

GPU acceleration approach (same for all seeders)

Runtime/iteration budgets

Metrics + logging format

GPU class held constant within comparisons (when possible) 

AI_HANDOFF_paper_implementation

Variable (the only experimental axis)

Quantum seeding engine (Random, QAOA, DCQO, DCQO+, PCE) 

Ai Handoff – Paper Research

2) “Recommended products” (what to generate as outputs)

Generate these artifacts in-repo:

Full codebase (module-based, not notebook-only)

CLI runner that produces reproducible logs/plots

Test suite with CPU oracle + CPU/GPU parity tests

Benchmark results folder structure (CSV/JSONL + figures)

Submission-ready docs

README.md (how to run, CPU vs GPU, scaling gates)

TEST_SUITE.md (what tests prove + how to run)

AI_REPORT.md (workflow + verification + vibe log template)

PRESENTATION_OUTLINE.md (slide-by-slide plan)

3) Environment assumptions (must be compatible)

Use only packages already in the default environment:

CUDA-Q present (cuda-quantum-cu12)

CuPy present (cupy-cuda12x)

Standard stack: numpy/scipy/matplotlib/networkx etc. 

environment_default

No extra pip installs unless absolutely necessary.

4) Repo layout to generate (exact)

Create this structure:

labs_hybrid_solver/
  pyproject.toml (optional) OR setup.cfg (optional)
  README.md
  src/
    labs_hybrid/
      __init__.py
      config.py
      rng.py
      types.py
      logging_utils.py
      labs_energy_cpu.py
      labs_energy_gpu.py
      selectors.py
      mts/
        __init__.py
        mts_cpu.py
        mts_gpu.py
      seeders/
        __init__.py
        random_seeder.py
        qaoa_seeder.py
        dcqo_seeder.py
        dcqo_plus_seeder.py
        pce_seeder.py
      pipeline.py
      benchmarks.py
      plots.py
  scripts/
    run_experiment.py
    run_bench_matrix.py
    make_plots.py
  tests/
    test_energy_smallN_bruteforce.py
    test_energy_cpu_gpu_parity.py
    test_seeders_validity.py
    test_mts_sanity.py
    test_dcqo_plus_trotter_knobs.py
  team_submissions/
    AI_REPORT.md
    TEST_SUITE.md
    PRESENTATION_OUTLINE.md
  results/   (gitignored, but create empty with placeholder README)

5) Core API contracts (must implement exactly)
5.1 Canonical representation

Represent sequences internally as ±1 int8 arrays of shape (N,) and populations (K, N).

Provide helpers to convert to/from {0,1} if needed for circuit measurement outputs.

5.2 Seeder interface (uniform across all seeders)

All seeders must implement:

def generate_seeds(
    N: int,
    shots: int,
    K_out: int,
    *,
    backend: str,         # "cpu" | "gpu" | "auto"
    rng_seed: int,
    params: dict
) -> tuple[np.ndarray, dict]:
    """
    Returns:
      seeds_pm1: (K_out, N) int8 in {-1, +1}
      info: dict with timings, raw stats, seeder hyperparams
    """

5.3 Selection step (post-selection)

Implement:

def select_topK_by_energy(seeds_pm1, K_select, energy_fn, xp=np|cupy):
    """Compute energy batched; return best K_select seeds and stats."""


This is important because DCQO’s value is often in the left tail of sampled energies. 

Ai Handoff – Paper Research

5.4 MTS interface

Implement:

def run_mts(
    seeds_pm1,
    *,
    budget_s: float | None,
    max_iters: int | None,
    tabu_params: dict,
    device: str,          # "cpu" | "gpu"
    rng_seed: int
) -> tuple[np.ndarray, float, dict]:
    """
    Returns:
      best_seq_pm1: (N,)
      best_energy: float
      trace/logs: dict (best-so-far curve, timings, counters)
    """

5.5 End-to-end pipeline

Implement:

def run_experiment(cfg) -> dict:
    """
    Orchestrates:
      seeder -> optional post-select -> MTS -> logging row -> artifacts
    Returns a JSON-serializable dict row for CSV/JSONL.
    """

6) Seeders to implement (must include all)

The seeder registry must include:

Random (sanity baseline)

QAOA (canonical baseline)

DCQO (Cadavid/Hegade baseline)

DCQO+ (trotter upgrades; no added depth)

PCE (Sciorilli twist seeder) 

Ai Handoff – Paper Research

 

AI_HANDOFF_paper_implementation

6.1 DCQO baseline seeder

Implement DCQO-style digitized evolution circuit as seeding primitive.

Keep it compatible with CUDA-Q execution.

Log compile time, sampling time, and distribution summary.

Reference intent: DCQO improves distribution of sampled solutions (not just a single best shot). 

Ai Handoff – Paper Research

6.2 DCQO+ seeder (trotterization upgrades)

DCQO+ must expose trotter knobs as config, implementing the three “zero-depth” upgrades from your DCQO trotterization document:

Baseline notebook behavior (must reproduce when knobs are off):

Lie–Trotter (first-order) product formula

right-endpoint sampling of time-dependent schedule

fixed Pauli ordering 

Dcqo Trotterization Upgrades

Upgrades (DCQO+)

Midpoint time discretization (right endpoint → midpoint)

Forward/reverse ordering alternation (deterministic averaging)

K-permutation ordering (randomized product formula “lite” across batches/runs) 

Dcqo Trotterization Upgrades

 

Dcqo Trotterization Upgrades

Implement config shape:

trotter_config = {
  "time_grid": "right_endpoint" | "midpoint",
  "ordering": "fixed" | "forward_reverse" | "k_permutation",
  "K_perm": int,              # only if k_permutation
}

6.3 PCE seeder (Pauli Correlation Encoding)

Implement PCE as a distinct seeder module with the same interface. This is the primary “twist” paper in the project handoff. 

Ai Handoff – Paper Research

Implementation notes:

If paper-specific circuit details are nontrivial, implement:

a faithful “skeleton” that captures the encoding pipeline and measurement to bitstrings,

plus explicit TODO markers where paper-specific constants/structures must be inserted,

but still returns valid seed bitstrings so the pipeline is runnable end-to-end.

Log qubit count, depth proxy, and sampling runtime for judge narrative.

7) GPU acceleration backbone (classical) — required
7.1 Design reference for GPU MTS

Use Zhang et al. as the design guide for structuring MTS to be massively parallel on GPUs. Do NOT copy solver-specific heuristics or custom CUDA kernels; use the principles (multi-start, batched evaluation, GPU-friendly representations). 

ZHANG_GPU_ACCELERATION_REFERENCE

 

ZHANG_GPU_ACCELERATION_REFERENCE

7.2 Minimum viable GPU implementation

labs_energy_gpu.py: batched energy evaluation in CuPy

mts_gpu.py: batched multi-start MTS loop that:

keeps populations on GPU

evaluates neighborhoods in batches

minimizes host/device transfer

Provide a CPU fallback (mts_cpu.py) for correctness and debugging.

8) Scaling plan (MUST be implemented in code + documented)

This must appear in both code and README as a gated workflow:

Gate 0 — CPU-only correctness

Run tiny N (≤10–12):

brute-force energy oracle tests

invariants tests

seeder validity tests

MTS sanity tests

Exit: all tests pass.

Gate 1 — CPU “small set” integration runs

Run end-to-end for small N (e.g., 12, 16):

Random, QAOA, DCQO, DCQO+
Verify logs + basic distribution sanity.

Exit: pipeline stable + deterministic.

Gate 2 — GPU bring-up (minimal token spend)

Energy CPU vs GPU parity tests

One small-N GPU run for each seeder backend path

Exit: GPU path works + parity checks pass.

Gate 3 — GPU benchmark matrix (main token spend)

Run a small set of N values (as suggested in the handoff doc):

N ∈ {20, 30, 40} (or adjust based on stability/time) 

Ai Handoff – Paper Research

Seeders: Random, QAOA, DCQO, DCQO+, PCE

Fixed budgets (time or iterations), fixed K seeds, fixed shots per seeder

Exit: CSV/plots produced + “time-to-best” curves ready.

Code requirement

Implement a BenchmarkPlan object in config.py that encodes:

N_list per gate

seeder list per gate

budgets per gate

device per gate

automatic stopping if GPU budget cap reached

Provide scripts/run_bench_matrix.py that executes Gate 0→3 in order (skipping earlier gates only if explicitly flagged).

9) Logging + plots (must be judge-friendly)
Logs

Write one row per run to CSV and JSONL containing:

N, seeder name, trotter_config (if applicable), shots, K_out, K_select

runtime breakdown:

seeder compile

seeder sampling

post-selection

MTS time

quality:

best seed energy

best final energy

best-so-far trace summary

diversity metrics (optional but recommended):

mean/median Hamming distance among top-K seeds 

Ai Handoff – Paper Research

Plots (auto-generated)

time-to-best curves by seeder

final energy vs budget bar chart

seed energy histogram (show tail advantage)

CPU vs GPU runtime breakdown (stacked or grouped)

10) Tests (must cover correctness + “DCQO+ is truly zero-depth”)

Implement tests:

test_energy_smallN_bruteforce.py

test_energy_cpu_gpu_parity.py

test_seeders_validity.py (shape/type/value constraints; determinism)

test_mts_sanity.py (final ≤ best seed under deterministic settings)

test_dcqo_plus_trotter_knobs.py:

baseline knobs reproduce baseline ordering/time-grid behavior

midpoint changes schedule sampling only

forward_reverse alternates ordering deterministically

k_permutation changes ordering across batches without changing term set/depth

Rationale: DCQO+ upgrades must add no extra gates/circuit depth and only change discretization/ordering. 

Dcqo Trotterization Upgrades

 

Dcqo Trotterization Upgrades

11) Submission docs content to generate
README.md

Must include:

architecture: quantum seeder → fixed GPU MTS → result

how to run:

Gate 0 tests

Gate 1 small-N demo

Gate 2 GPU parity

Gate 3 benchmark matrix

how to switch backend: cpu/gpu/auto

expected outputs: results CSV + plots

attribution statement (clean separation of variables) 

AI_HANDOFF_paper_implementation

team_submissions/TEST_SUITE.md

What each test proves

How to run tests locally

Parity philosophy: CPU as oracle; GPU must match

team_submissions/AI_REPORT.md

Include templated sections:

agent workflow

verification strategy

vibe log (win/learn/fail)

“red lines” (don’t tune MTS per seeder; don’t change budgets) 

AI_HANDOFF_paper_implementation

team_submissions/PRESENTATION_OUTLINE.md

Slide plan:

Problem + architecture

Why fixed MTS and interchangeable seeders

DCQO baseline vs DCQO+ (trotter upgrades)

PCE twist seeder

GPU acceleration (Zhang-inspired) makes scaling measurable

Results plots

Takeaways + next steps 

Ai Handoff – Paper Research

 

ZHANG_GPU_ACCELERATION_REFERENCE

12) Paper references (must be included in README + AI report)
Seeding methods (core)

Gómez‑Cadavid et al. (2025): QE-MTS / DCQO seeding baseline; defines architecture + evaluation metrics. 

Ai Handoff – Paper Research

Hegade et al. (2022): Digitized Counterdiabatic Quantum Optimization (DCQO) mechanism justification. 

Ai Handoff – Paper Research

Sciorilli et al. (2025): PCE (Pauli Correlation Encoding) = primary “twist” seeder. 

Ai Handoff – Paper Research

GPU acceleration (classical backbone)

Zhang et al. (2025): massively parallel memetic tabu search for LABS; used as GPU design reference (principles, not heuristics/kernels). 

ZHANG_GPU_ACCELERATION_REFERENCE

Trotterization upgrades used in DCQO+ (zero added depth)

Ikeda et al. (2023): midpoint/time-dependent product formula accuracy improvements. 

Dcqo Trotterization Upgrades

Childs et al. (2021): commutator-scaling view of Trotter error; supports discretization considerations. 

Dcqo Trotterization Upgrades

Childs et al. (2019): randomized product formulas; motivates k-permutation ordering. 

Dcqo Trotterization Upgrades

(These exact references and the “why not higher-order Suzuki–Trotter” rationale are summarized in your DCQO+ doc.) 

Dcqo Trotterization Upgrades

13) Acceptance checklist (definition of done)

Codebase is “done” when:

pytest passes Gate 0

Gate 1 runs end-to-end on CPU and produces logs/plots

Gate 2 passes CPU/GPU energy parity and produces at least one GPU run

Gate 3 benchmark matrix runs and produces:

CSV/JSONL logs

time-to-best plot

final energy plot

seed histogram plot

README + submission docs are complete and consistent with the fixed-vs-variable framingAdd-on paragraph: Results handoff back to the code-generating AI

Results Feedback Loop (Required): After running any gate (Gate 0–3), the code must produce a single portable “handoff bundle” that can be pasted or uploaded back into the AI chat that generated the code. Concretely: at the end of each run, write (1) a results_summary.md (human-readable), (2) a run_config.json (exact config used), (3) a metrics.csv (one row per run), and (4) a artifacts/ folder containing key plots (PNG) and minimal logs (JSONL). Also write a PROMPT_BACK_TO_AI.md file that contains a ready-to-paste prompt plus the most important metrics (best energies, runtimes, parity test status, and any failures). The prompt must instruct the AI to: (a) verify correctness regressions first (tests/parity), (b) diagnose performance bottlenecks using the runtime breakdown fields, and (c) propose the smallest code edits that improve either correctness or speed while preserving the fixed-vs-variable rule (MTS fixed; only seeders change). This creates a repeatable “run → bundle → paste back to AI → patch” loop.

If you want one more line to make it operational, add: “scripts/package_handoff.py must generate a timestamped zip of the handoff bundle.” ok this is information from another chat instructin gyou how to proceed with building all the proeducs we need to make this project happen. Here is the plan. Make the zip

## 6) Notes for judges
- All five seeders (Random, QAOA, DCQO, DCQO+, PCE) are fully implemented and benchmarked through the same fixed MTS backbone.
- **PCE is paper-accurate** (Sciorilli et al. 2025): numpy + scipy classical simulation with brickwork ansatz, relaxed LABS cost, L-BFGS-B optimization. No CUDA-Q dependency — uses n = ceil(log₄(N+1)) qubits with ~30 restarts.
- **All 9 revision items completed**: GPU energy kernel, GPU MTS delta-energy, PCE completion, convergence parity test, CUDA-Q seeding verification, OOM guard, post-selection tests, fallback documentation, MTS stagnation detection.
- **GPU acceleration benchmarks** (L4 GPU): energy kernel 150–250× speedup (31–40 µs constant), MTS delta-energy 13–48× speedup (O(K·N²) vs O(K·N³)).
- **Gate 2 results** (N=16): PCE achieves raw energy minimum of 24 (vs 32–36 for QAOA/DCQO), demonstrating superior initial seed quality. All seeders converge to final energy 24 after MTS.
- **30 tests** across 8 files ensure correctness at every layer: brute-force oracle, CPU/GPU parity, kernel-level parity, seeder validity, MTS sanity, trotter knob invariants, and post-selection correctness.
